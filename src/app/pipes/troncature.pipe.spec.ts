import { TroncaturePipe } from './troncature.pipe';

describe('TroncaturePipe', () => {
  it('create an instance', () => {
    const pipe = new TroncaturePipe();
    expect(pipe).toBeTruthy();
  });
});
